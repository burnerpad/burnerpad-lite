[defaults]
inventory = inventory.ini
# Galaxy installs into the FIRST roles_path entry, so keep it separate from this repo's own roles:
# third-party roles land in .roles/ (gitignored, like .collections/), ours stay in roles/ (committed).
roles_path = .roles:roles
collections_path = .collections
retry_files_enabled = False
host_key_checking = True
