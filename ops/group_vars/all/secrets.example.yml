# Copy to secrets.yml (GITIGNORED) and fill in. NEVER commit the real file.
# Stronger: `ansible-vault encrypt secrets.yml` so it's encrypted even if it ever leaks.
cloudflare_tunnel_token: "CHANGE_ME"          # Cloudflare Zero Trust → Tunnels → token
cloudflare_zone_id: "CHANGE_ME"               # production zone ID; keep private with operator evidence
cloudflare_rulesets_read_token: "CHANGE_ME"   # read-only Rulesets token scoped to the production zone
tailscale_auth_key: "tskey-auth-CHANGE_ME"    # Tailscale admin → Keys (one-off, for first bootstrap)
heartbeat_url: "CHANGE_ME"                    # required HTTPS dead-man's-switch ping URL
